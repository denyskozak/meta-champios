"use client";
import React from "react";

import { Login } from "@/components/login";

const LoginPage = () => {
  return (
    <section className="flex items-center flex-col justify-center w-screen h-screen">
      <Login />
    </section>
  );
};

export default LoginPage;
